name: Wine MSYS2 Docker

on:
  push:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # - uses: actions/checkout@v3
      - name: Download artifact
        id: download-artifact
        uses: dawidd6/action-download-artifact@v2
        with:
          workflow: wine-msys2.yml
          workflow_conclusion: success
          branch: master-msys2
          path: /opt
      - name: Build Docker Image
        run: |
          sudo apt update
          sudo apt install debootstrap perl git wget xz-utils bubblewrap autoconf
          #
          echo _ cd /opt
          cd /opt
          #
          echo _ ls -alh /opt
          ls -alh /opt
          #
          echo _ tree
          tree
          #
          echo cd
          cd "Wine Stable"
          #
          echo _ sudo tar -xpf /opt/wine-*-custom-amd64.tar.xz
          sudo tar -C -xpf /opt/wine-*-custom-amd64.tar.xz
          #
          echo _ ls -alh /opt
          ls -alh /opt
          # 
          echo _ tree
          tree
          #
          
      # - uses: actions/upload-artifact@v3
      #   with:
      #     name: Wine Stable
      #     path: ./*.tar.xz
      #     retention-days: 2
